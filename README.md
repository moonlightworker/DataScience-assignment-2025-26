The first part is "Pangaea_data_1.ipynb" (created in Jupyter notebook), where we build a search query and download metadata and datasets from PANGAEA.
The second part is "Pangaea_data_2.Rmd" (created in R Studio), where we process the datasets and visualize them. 
The compiled results of both scripts are the .html files.
